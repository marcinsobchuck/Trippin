export enum Routes {
  Home = "/",
  Entry = "/entry",
  ForgottenPassword = "/forgotten-password",
  Route = "/route",
  Everywhere = "/everywhere",
  Wishlist = "/wishlist",
  EditUser = "/edit-user",
}
